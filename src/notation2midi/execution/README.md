The Execution class processes metadata directives for flow (GOTO, SEQUENCE, LOOP) and interpretation (TEMPO, DYNAMICS).
These elaborations together with the contents of the Score object contain all the information needed to generate the MIDI messages.